# diabetes_logistic_regression_model
This project implements a Machine Learning model using Logistic Regression to predict whether a patient is diabetic or not based on medical diagnostic features. The model is trained and evaluated on a structured dataset and demonstrates a complete ML workflow including data preprocessing, training, testing, and performance evaluation.
